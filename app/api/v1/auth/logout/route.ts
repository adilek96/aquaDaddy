import { NextResponse } from "next/server";

/**
 * Токены мобильного приложения не хранятся на сервере, поэтому выход —
 * это просто удаление токена на клиенте. Роут существует, чтобы приложение
 * получало 200 вместо 405 и могло в будущем дописывать сюда отзыв токена.
 */
export async function POST() {
  return NextResponse.json({ success: true });
}
